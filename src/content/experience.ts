/**
 * Блок «На площадке».
 *
 * Здесь два режима, и переключаются они сами по данным:
 *
 *  • `events` заполнен  — показываем первые VISIBLE_COUNT реальных
 *    мероприятий, остальные раскрываются кнопкой «Показать больше».
 *  • `events` пуст      — показываем форматы съёмки и монтажа. Кнопки
 *    раскрытия при этом нет: раскрывать нечего.
 *
 * ⚠️ Подтверждённого списка мероприятий пока нет. Прежние десять событий
 * с годами 2023–2026 и «300+ гостей» были выдуманы и удалены — вписывать
 * их обратно нельзя.
 */

export type Event = {
  title: string;
  year: string;
  role: string;
  scale?: string;
};

/** Сколько строк видно до нажатия «Показать больше». */
export const VISIBLE_COUNT = 6;

export const experience = {
  title: 'На площадке',
  /** Заполнить реальными мероприятиями — тогда список включится сам. */
  events: [] as Event[],
  fallbackLabel: 'Форматы съёмки и монтажа',
  fallbackFormats: [
    'Мероприятия',
    'Fashion',
    'Многокамерная съёмка',
    'Репортаж',
    'Интервью',
    'Вертикальный контент',
  ],
};
